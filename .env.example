# VAPID credentials for Web Push notifications
# Generate with: npx web-push generate-vapid-keys
# NEVER commit your actual .env file — only this example.

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:weathergpt@example.com
